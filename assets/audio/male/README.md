# Male voice

Drop male-voice recordings here, named `{letter}.mp3`, one per letter
listed in `NBackDefaults.audioLetters` (currently `c h k l q r s t`).

Until files are present, choosing "Male" in Settings → Sound is silent
on the audio channel — `AudioService.playLetter` no-ops for missing
assets. The rest of the game continues to work.
